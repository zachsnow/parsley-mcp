name = "parsley-mcp"
main = "src/worker.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

routes = [
  { pattern = "parsley.vein.io", custom_domain = true }
]

rules = [
  { type = "Text", globs = ["**/*.html"] }
]
