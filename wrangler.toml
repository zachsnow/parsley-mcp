name = "parsely-mcp"
main = "src/worker.ts"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENABLE_WRITES = "false"

routes = [
  { pattern = "parsely.vein.io", custom_domain = true }
]
